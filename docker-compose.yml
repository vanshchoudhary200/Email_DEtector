services:
  mongo:
    image: mongo:7
    container_name: suspicious-email-detector-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data:
